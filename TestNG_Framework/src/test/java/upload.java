public class upload {
    Implicit =10 sec
    String filepath = "c/user/abc.xlsx";
    webElement uploadBtn = driver.findelement(By.xpath());
    uploadBtn.click();
    WebElemt upload = driver.findelement(By.xpath());
    Thread.sleep()
    upload.sendKeys(filepath);
     Thread.sleep()
    //if this method will not work then we need some external plugins
    1. AutoIT
    2. Robot class

   wait
    1. Implicit
    2. explicit
    3. Fluent wait
}
